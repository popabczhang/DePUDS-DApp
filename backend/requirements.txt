Flask==2.3.3
flask-cors==3.0.10
Flask-SQLAlchemy>=3.0 # 升级 Flask-SQLAlchemy 到 3.x 或更高版本
flask-jwt-extended==4.4.4
web3==5.28.0
python-dotenv==0.19.2
Werkzeug>=3.0 # Werkzeug 3.x 可能与 Flask 2.3.x 有兼容性要求，检查 Flask 文档
Jinja2>=3.0 # Jinja2 3.x 可能与 Flask 2.3.x 有兼容性要求
itsdangerous>=2.0 # itsdangerous 2.x 可能与 Flask 2.3.x 有兼容性要求
click>=8.0 # click 8.x 可能与 Flask 2.3.x 有兼容性要求
blinker>=1.6 # blinker 1.6+ 可能与 Flask 2.3.x 有兼容性要求
# SQLAlchemy 会作为 Flask-SQLAlchemy 的依赖自动安装
